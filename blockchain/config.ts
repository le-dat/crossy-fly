import { baseSepolia } from "viem/chains";

export const CHAIN = baseSepolia;

// Deploy CrossyfluffleGame.sol via Remix (https://remix.ethereum.org)
// on Base Sepolia, then paste the contract address here.
export const CONTRACT_ADDRESS = "0x0000000000000000000000000000000000000000" as `0x${string}`;
